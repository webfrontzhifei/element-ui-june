import DateRangePicker from '../date-picker/src/date-range-picker.vue';

DateRangePicker.install = (Vue) => {
    Vue.component(DateRangePicker.name, DateRangePicker);
};

export default DateRangePicker;
